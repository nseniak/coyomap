{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://coyomap.dev/schemas/project-map.schema.json",
  "title": "coyomap project map",
  "description": "Auto-generated from tools/coyomap/model.py \u2014 documentation and IDE-autocomplete use only; NOT used by `coyomap validate` (see this module's docstring, and method/model.md, for why). Regenerate with `python -m coyomap.json_schema > method/project-map.schema.json`.",
  "type": "object",
  "properties": {
    "format": {
      "type": "string",
      "const": "coyomap-map"
    },
    "title": {
      "type": "string"
    },
    "goal": {
      "type": "string"
    },
    "commit": {
      "type": [
        "string",
        "null"
      ],
      "description": "short commit sha the map was built at."
    },
    "committed": {
      "type": [
        "string",
        "null"
      ],
      "description": "YYYY-MM-DD."
    },
    "built": {
      "type": [
        "string",
        "null"
      ],
      "description": "YYYY-MM-DD HH:MM."
    },
    "tool_commit": {
      "type": [
        "string",
        "null"
      ],
      "description": "short commit sha of the COYOMAP build that produced this map \u2014 not the analysed repo's. Two maps are only comparable against the tools that made them; absent on maps built before this was stamped."
    },
    "tool_committed": {
      "type": [
        "string",
        "null"
      ],
      "description": "YYYY-MM-DD."
    },
    "roles": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Role"
      }
    },
    "glossary": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/GlossaryRow"
      }
    },
    "capabilities": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Group"
      }
    },
    "use_cases": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/UseCase"
      }
    },
    "happy_path": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/HappyStep"
      }
    },
    "subsystems": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Group"
      }
    },
    "components": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Component"
      }
    },
    "deps": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Dep"
      }
    },
    "interfaces": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Interface"
      }
    },
    "run_commands": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/RunRow"
      }
    },
    "entry_points": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/EntryPoint"
      }
    },
    "subdomains": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Group"
      }
    },
    "entities": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Entity"
      }
    },
    "non_entity_types": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/NonEntityType"
      }
    },
    "flows": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Flow"
      }
    },
    "subflows": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/SubFlow"
      }
    },
    "edges": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Edge"
      }
    },
    "messaging": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/MessagingRow"
      }
    },
    "deployment": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/DeploymentRow"
      }
    },
    "environments": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "observability": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/ObservabilityRow"
      }
    },
    "security": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/SecurityRow"
      }
    },
    "config": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/ConfigRow"
      }
    },
    "tests_note": {
      "type": "string"
    },
    "tests": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/TestRow"
      }
    },
    "grounding": {
      "$ref": "#/$defs/Grounding"
    },
    "blocks": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Group"
      }
    },
    "rules": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/BusinessRule"
      }
    },
    "extras": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/ExtraSection"
      }
    }
  },
  "required": [],
  "additionalProperties": false,
  "$defs": {
    "Role": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^R\\d+$",
          "description": "a role is a first-class element (`R<n>`), referenced by id \u2014 a use case's `actors` and a flow's actor steps carry role ids."
        },
        "name": {
          "type": "string"
        },
        "kind": {
          "type": "string",
          "description": "human | service | ai-agent, free text (not a closed vocabulary). `ai-agent` is a program driven by a language model, acting for somebody, and is ALWAYS outside the product \u2014 only `service` with an `internal` audience is the product's own scheduled work."
        },
        "audience": {
          "type": "string",
          "enum": [
            "",
            "user",
            "internal"
          ],
          "description": "WHICH SIDE of the product this actor sits on. `internal` = the side of the company that ships it; `user` = everyone else, including someone who has not bought it yet. Same question of a person and of a program, answered from different evidence: a PERSON is `internal` when they work for that company; a PROGRAM is judged by WHOSE MACHINE IT IS, never by whose work it happens to be doing \u2014 the customer set it up (`user`), or the company runs it OR PAYS A VENDOR to run it (`internal`). A payment provider, an email sender or any other bought service is `internal`: no customer configured it. The map's ONE authored answer to 'who is this for' \u2014 a capability's audience is DERIVED from the roles driving its use cases, never authored, so the two can never contradict. Orthogonal to `kind`."
        },
        "wants": {
          "type": "string"
        },
        "drives": {
          "type": "string",
          "description": "the use cases this role drives \u2014 free text, ids inside."
        },
        "relations": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/RoleRelation"
          },
          "description": "optional links to other roles, for when one human typically holds several of the map's roles. Roles are permission hats the code recognizes; without a relation the viewer draws one person as several strangers. Empty/absent = no relation stated."
        }
      },
      "required": [
        "id",
        "name"
      ],
      "additionalProperties": false
    },
    "RoleRelation": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "becomes",
            "includes"
          ],
          "description": "`becomes` = this role turns into `role` at the use case `at` (the transition is a real, mapped action \u2014 e.g. the prospect becomes the admin at 'sign in and name the organization'). `includes` = this role may do everything `role` may do (the admin includes the member)."
        },
        "role": {
          "type": "string",
          "pattern": "^R\\d+$",
          "description": "the other role \u2014 must be a defined Role id."
        },
        "at": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^UC\\d+$",
          "description": "becomes only: the use case where the hat changes \u2014 must be a defined use-case id."
        },
        "source": {
          "type": [
            "string",
            "null"
          ],
          "description": "`includes` only: the `path:line` that GRANTS the inclusion \u2014 the check that lets this role do what the other may do. An `includes` is an access claim and the viewer draws it as one, so it carries evidence like every other access claim. Null is allowed and says nobody has anchored it; `audit` then challenges it as an unanchored access claim rather than passing it in silence."
        }
      },
      "required": [
        "kind",
        "role"
      ],
      "additionalProperties": false
    },
    "GlossaryRow": {
      "type": "object",
      "properties": {
        "term": {
          "type": "string"
        },
        "meaning": {
          "type": "string"
        },
        "source": {
          "type": [
            "string",
            "null"
          ],
          "description": "either a bare file `path:line` anchor (see `evidence[].file`'s description) or a bare directory ref ending in `/`. The term's canonical code home (where it is defined); null when the concept has no single code home (a pure product-level term)."
        },
        "aliases": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "extra surface forms the viewer also links to this term's definition. Real alternative names only, unambiguous in this project's prose \u2014 never a bare generic English word, and never a plural/possessive/case/hyphen variant (the viewer folds those automatically). See the Glossary deliverable in method.md."
        },
        "no_autolink": {
          "type": "boolean",
          "description": "true = the term's own name is excluded from automatic in-prose linking; it links only via its aliases (or not at all). For a term whose name is an ordinary English word that would over-link."
        }
      },
      "required": [
        "term"
      ],
      "additionalProperties": false
    },
    "Group": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^(UC|HP|SD|SF|CAP|EP|BLK|BR|C|D|E|I|S|R)\\d+$",
          "description": "`S<n>` in subsystems[], `SD<n>` in subdomains[], `CAP<n>` in capabilities[], `BLK<n>` in blocks[] \u2014 same dataclass, four id forests."
        },
        "name": {
          "type": "string"
        },
        "purpose": {
          "type": "string"
        },
        "parent": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^(UC|HP|SD|SF|CAP|EP|BLK|BR|C|D|E|I|S|R)\\d+$",
          "description": "the enclosing group's id, in the SAME forest (an S parents an S, an SD an SD, a CAP a CAP, a BLK a BLK), or null for top-level."
        },
        "happy_path": {
          "type": "string",
          "enum": [
            "",
            "expected",
            "excluded"
          ],
          "description": "CAPABILITY-ONLY: must the happy path reach this capability? `expected` = yes, at least one of its use cases; `excluded` = no, and one 'Happy Path coverage' record says why. Deliberately NOT derived from happy_path[] \u2014 a value that always agreed with the happy path could never disagree with it, and the disagreement IS the check. Says nothing about audience. `validate` blocks it on a subsystem or a subdomain."
        },
        "stakes": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Stake"
          },
          "description": "CAPABILITY-ONLY: one entry per driving actor, saying what THAT actor comes to this capability to do. The Features diagram labels each actor\u2192feature arrow with the actor's stake; a capability's purpose alone is written from one chair and hides the other roles. `validate` blocks it on the other forests, and advises when a derived driving actor has no entry."
        },
        "story": {
          "$ref": "#/$defs/StoryAnchor",
          "description": "CAPABILITY-ONLY, and only worth authoring on a feature the happy path never reaches: where that feature sits in the ONE story column the viewer draws. The happy path reads unbroken and the off-happy-path features form a block after it, so `after` ORDERS that block (the block is already after every happy-path feature) while `before` is the one placement that keeps a feature among the happy path \u2014 author it on a lead-in like a marketing page, which belongs BEFORE the first step. Absent = the viewer guesses from the feature's actors' last happy-path step, which orders the block sensibly for trailing features (ops, a chat variant of work already walked) and never rescues a lead-in. `validate` blocks it on the other forests."
        },
        "owners": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "pattern": "^CAP\\d+$"
          },
          "description": "SUB-DOMAIN-ONLY: which FEATURE this data area exists for \u2014 the one that creates its records and runs their lifecycle. AUTHORED, never derived: every derivation was measured on three live maps and each guessed from what the code TOUCHES, which is not what the data is FOR (snapshots are first written by page tracking, but they exist so change detection can compare them). One id = that feature owns the area; several = deliberately shared among exactly those; field ABSENT = the decision has not been made, and an advisory asks for it. `[]` is a shape error, not an answer. NEVER author one to complete a diagram: an owner the area's records are never reached by is reported as an owner with no evidence. `validate` blocks it on a subsystem, a capability or a block, and cross-examines the list against the derived touches."
        },
        "specified_under": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "pattern": "^CAP\\d+$"
          },
          "description": "BLOCK-ONLY: the FEATURE(s) a person writing the product's spec would put these rules under. TWO STANDPOINTS, and this field is the first: SPEC (under which feature would these rules be WRITTEN) versus ENFORCEMENT (in which feature's code are they APPLIED). They differ on most areas \u2014 mcpolis's 'Who may call which tool' is specified under Access control and enforced inside the gateway; its 'Plan caps' is applied across six features and specified under a plans feature that product has not shipped, so the field is absent there. The ENFORCEMENT answer is already derivable (a rule's call sites resolve to components, and its steps join to use cases and so to features), which is what settles it: nothing in the code says where a rule would be WRITTEN IN A SPEC, and that judgement is the one a person has to make. AUTHORED, like `owners` one field up, and for the same measured reason \u2014 every derivation was tried on three live maps and every one failed (the derived top feature holds 52% and 61% of an area's links at the median; 8 of 32 areas win on under half). NOT `owners`, which is about DATA. The Rules page groups by it, and inherits the story column's order for free. One id; several if it genuinely spans them; ABSENT = not decided, and an advisory asks. `[]` is a shape error. `validate` blocks it on the other three forests and cross-examines it against the features the area's rules reach."
        },
        "source": {
          "type": [
            "string",
            "null"
          ],
          "description": "either a bare file `path:line` anchor (see `evidence[].file`'s description) or a bare directory ref ending in `/`. The group's home directory (or a representative file)."
        },
        "confidence": {
          "type": "string",
          "enum": [
            "verified",
            "inferred",
            ""
          ],
          "description": "verified = read in the code; inferred = deduced. '' = unstated."
        },
        "tech": {
          "type": "string",
          "description": "SUBSYSTEM-ONLY: one honest stack label ('Python/FastAPI', 'Go', 'Elixir') read off the manifests \u2014 not a stack essay. validate blocks it on a subdomain (a bounded context has no stack)."
        },
        "tech_source": {
          "type": "string",
          "pattern": "^\\S+\\.\\w+(?::\\d+(?:-\\d+)?)?$|^\\S+:\\d+(?:-\\d+)?$",
          "description": "optional bare path:line anchor to the manifest line proving the tech label (go.mod, package.json, pyproject.toml)."
        }
      },
      "required": [
        "id",
        "name"
      ],
      "additionalProperties": false
    },
    "Stake": {
      "type": "object",
      "properties": {
        "actor": {
          "type": "string",
          "pattern": "^R\\d+$",
          "description": "the driving role this stake belongs to \u2014 must be a defined Role id."
        },
        "stake": {
          "type": "string",
          "description": "a short verb phrase with the actor as the implied subject ('mounts, configures and runs the servers'). Must read correctly after the actor's name; writing rules apply (one idea, plain words, no code, no step numbers)."
        }
      },
      "required": [
        "actor"
      ],
      "additionalProperties": false
    },
    "StoryAnchor": {
      "type": "object",
      "properties": {
        "place": {
          "type": "string",
          "enum": [
            "before",
            "after"
          ],
          "description": "exactly `before` or `after` (lowercase) \u2014 the derivation and `validate` compare strictly."
        },
        "feature": {
          "type": "string",
          "pattern": "^CAP\\d+$",
          "description": "the feature this one reads beside \u2014 must be a defined capability id, never the capability itself."
        }
      },
      "required": [
        "place",
        "feature"
      ],
      "additionalProperties": false
    },
    "UseCase": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^UC\\d+$"
        },
        "name": {
          "type": "string"
        },
        "actors": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "trigger": {
          "type": "string",
          "description": "what STARTS this use case, in one plain sentence."
        },
        "outcome": {
          "type": "string",
          "description": "what the actor COMES AWAY WITH, in one plain sentence. The pair is the use case's outside face; its flow is the inside one. Never join the two into one cell."
        },
        "capability": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^CAP\\d+$",
          "description": "the capability this use case belongs to, or null. Assigned at synthesis via `reconcile` (a `CAP<n>` does not exist when the behavioral fragment is written)."
        },
        "entry_points": {
          "type": "array",
          "items": {
            "pattern": "^EP\\d+$"
          },
          "description": "the TRIGGER arm of entry-point claiming: the front door(s) an actor hits to START this use case. Empty is legitimate \u2014 the surface may sit inside a coarse T4 row a sibling already names."
        }
      },
      "required": [
        "id",
        "name"
      ],
      "additionalProperties": false
    },
    "HappyStep": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^HP\\d+$",
          "description": "this step's position in the happy path."
        },
        "uc": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^UC\\d+$",
          "description": "the use case this step realizes."
        },
        "why": {
          "type": [
            "string",
            "null"
          ],
          "description": "the prerequisite that fixes this step's position \u2014 why it can't come earlier in the happy path."
        }
      },
      "required": [
        "id"
      ],
      "additionalProperties": false
    },
    "Component": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^C\\d+$"
        },
        "name": {
          "type": "string"
        },
        "subsystem": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^S\\d+$",
          "description": "the owning subsystem's id, or null if ungrouped."
        },
        "purpose": {
          "type": "string"
        },
        "depends_on": {
          "type": "string"
        },
        "source": {
          "type": [
            "string",
            "null"
          ],
          "description": "either a bare file `path:line` anchor (see `evidence[].file`'s description) or a bare directory ref ending in `/`. Where the component LIVES."
        },
        "confidence": {
          "type": "string",
          "enum": [
            "verified",
            "inferred",
            ""
          ],
          "description": "verified = read in the code; inferred = deduced. '' = unstated."
        },
        "files": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "repo-relative file paths this component owns, as a plain list \u2014 not a count, not a comma-joined string."
        },
        "runs_in": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "evidence": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/EvidenceItem"
          }
        },
        "states": {
          "$ref": "#/$defs/StateMachine"
        },
        "extra": {
          "type": "object",
          "description": "freeform authored columns \u2014 any JSON value, agent-chosen keys. The ONE place with no fixed meaning: a key `coyomap validate` gives an enforced shape to, or that the method documents as a convention, graduates to a real field instead and is then rejected here under its old spelling (this is how `files`/`evidence`/`package`/`alternative` were promoted)."
        }
      },
      "required": [
        "id",
        "name"
      ],
      "additionalProperties": false
    },
    "EvidenceItem": {
      "type": "object",
      "properties": {
        "file": {
          "type": "string",
          "pattern": "^\\S+\\.\\w+(?::\\d+(?:-\\d+)?)?$|^\\S+:\\d+(?:-\\d+)?$",
          "description": "bare `path:line` anchor: a repo-relative file path, optionally followed by `:line` or `:line-line` \u2014 never a markdown link (its label would just be the basename, fully derivable from the path, so it is never authored)."
        },
        "why": {
          "type": "string",
          "description": "why this citation supports the claim \u2014 what a skeptic re-reading `file` should find true."
        }
      },
      "required": [
        "file",
        "why"
      ],
      "additionalProperties": false
    },
    "StateMachine": {
      "type": "object",
      "properties": {
        "states": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "the declared state names \u2014 non-empty, unique; never synthesized (record only a lifecycle the code implements in an enum / status constants / dispatch table)."
        },
        "transitions": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/StateTransition"
          }
        },
        "source": {
          "type": "string",
          "pattern": "^\\S+\\.\\w+(?::\\d+(?:-\\d+)?)?$|^\\S+:\\d+(?:-\\d+)?$",
          "description": "bare path:line anchor to the line DECLARING the states; '' = inferred (advisory)."
        }
      },
      "required": [
        "states"
      ],
      "additionalProperties": false
    },
    "StateTransition": {
      "type": "object",
      "properties": {
        "src": {
          "type": "string"
        },
        "dst": {
          "type": "string"
        },
        "on": {
          "type": "string",
          "description": "the trigger label ('connect ok', 'refresh failed'); optional."
        }
      },
      "required": [
        "src",
        "dst"
      ],
      "additionalProperties": false
    },
    "Dep": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^D\\d+$"
        },
        "name": {
          "type": "string"
        },
        "kind": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "datastore",
            "messaging",
            "service",
            "platform",
            "framework",
            "library",
            null
          ],
          "description": "closed Context vocabulary; null \u2192 inferred from `type`."
        },
        "type": {
          "type": "string"
        },
        "used_for": {
          "type": "string"
        },
        "bucket": {
          "type": "string",
          "description": "PURPOSE bucket (seeded-open) grouping the dep within its diagram \u2014 external systems in Context, in-process code in the Libraries drill. Prefer a seed (Data & storage, Identity & access, Observability, Messaging & delivery, AI & ML, Infrastructure & runtime, Integrations for external systems; Web framework / server, Frontend / UI, Data drivers, Service SDKs, Validation / models, Logging, Crypto / security for libraries); mint a new one only when none fits. Empty \u2192 inferred from `type` + `used_for`."
        },
        "where_configured": {
          "type": "string",
          "pattern": "^\\S+\\.\\w+(?::\\d+(?:-\\d+)?)?$|^\\S+:\\d+(?:-\\d+)?$",
          "description": "bare `path:line` anchor: a repo-relative file path, optionally followed by `:line` or `:line-line` \u2014 never a markdown link (its label would just be the basename, fully derivable from the path, so it is never authored)."
        },
        "confidence": {
          "type": "string",
          "enum": [
            "verified",
            "inferred",
            ""
          ],
          "description": "verified = read in the code; inferred = deduced. '' = unstated."
        },
        "deployment_linked": {
          "type": "boolean"
        },
        "package": {
          "type": "string",
          "description": "one string: \"<name> <version> (<where declared>)\"."
        },
        "alternative": {
          "type": "string",
          "description": "the fallback used instead of this dep, and under what circumstance."
        },
        "evidence": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/EvidenceItem"
          }
        },
        "interfaces": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^I\\d+$"
          },
          "description": "the interfaces this dep belongs to, on EITHER side of each: the dep may BE the surface (an error tracker) or sit on its FAR SIDE (a coding agent that calls our skill). A list, because one outside system really does sit on several surfaces. `side` is authored on the interface, never inferred from this."
        },
        "not_an_interface": {
          "type": "string",
          "description": "why this dep is no interface at all \u2014 REQUIRED on a datastore/messaging/service/platform dep that names no interface. Without it there is no way to tell 'deliberately not one' from 'nobody looked', which is the trap a search service sets: a search over the product's own records is not an interface, a search over the open web is, and the call site looks identical."
        },
        "extra": {
          "type": "object",
          "description": "freeform authored columns \u2014 any JSON value, agent-chosen keys. The ONE place with no fixed meaning: a key `coyomap validate` gives an enforced shape to, or that the method documents as a convention, graduates to a real field instead and is then rejected here under its old spelling (this is how `files`/`evidence`/`package`/`alternative` were promoted)."
        }
      },
      "required": [
        "id",
        "name"
      ],
      "additionalProperties": false
    },
    "Interface": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^I\\d+$"
        },
        "name": {
          "type": "string",
          "description": "the surface in PRODUCT words ('Customer dashboard'), never a code word ('http-route')."
        },
        "what": {
          "type": "string",
          "description": "one sentence: what this surface is for."
        },
        "side": {
          "type": "string",
          "enum": [
            "ours",
            "theirs"
          ],
          "description": "whose DESIGN the surface is: if the far side vanished tomorrow, would the shape of this thing change? Our command line keeps its shape; our call to a payment processor does not."
        },
        "facing": {
          "type": "string",
          "enum": [
            "user",
            "operator",
            ""
          ],
          "description": "who it serves. AUTHORED \u2014 `Role.audience` answers a different question (which side of the COMPANY an actor sits on) and marks a bought payment service 'internal' while its interface is user-facing."
        },
        "kind": {
          "type": "string",
          "description": "WHAT THIS SURFACE IS, seeded-open: screen/mobile-app/desktop-app/command-line/file/settings/message/hosted-screen/content/handoff/api/agent-tools/mcp. WHAT IT IS, never WHAT IT IS FOR \u2014 a payment processor and a crash reporter are both `api`, and `Dep.bucket` already says which is which in a richer vocabulary. A kind that answers 'what is it for' is mis-modelled. Prefer a seed; mint only when none fits, and reuse the exact spelling on rebuild."
        },
        "ways_in": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^EP\\d+$"
          },
          "description": "the entry points this surface is made of. Assigned through `reconcile` (`ways_in`), never hand-written into a fragment: `EPn` ids are minted by `assemble` from content. Empty is legitimate \u2014 a `theirs` surface has none, and neither do the files a product writes or the settings an operator sets."
        },
        "source": {
          "type": "string",
          "pattern": "^\\S+\\.\\w+(?::\\d+(?:-\\d+)?)?$|^\\S+:\\d+(?:-\\d+)?$",
          "description": "the one line that declares the SURFACE \u2014 the router, the command table, the file writer. Advisory, never required: a settings surface is declared in no single place."
        },
        "confidence": {
          "type": "string",
          "enum": [
            "verified",
            "inferred",
            ""
          ],
          "description": "verified = read in the code; inferred = deduced. '' = unstated."
        },
        "evidence": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/EvidenceItem"
          }
        }
      },
      "required": [
        "id",
        "name"
      ],
      "additionalProperties": false
    },
    "RunRow": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string"
        },
        "command": {
          "type": "string"
        },
        "source": {
          "type": "string",
          "pattern": "^\\S+\\.\\w+(?::\\d+(?:-\\d+)?)?$|^\\S+:\\d+(?:-\\d+)?$",
          "description": "bare `path:line` anchor: a repo-relative file path, optionally followed by `:line` or `:line-line` \u2014 never a markdown link (its label would just be the basename, fully derivable from the path, so it is never authored). Where the run command is defined \u2014 the script, Makefile target, or config line the action runs."
        }
      },
      "required": [
        "action"
      ],
      "additionalProperties": false
    },
    "EntryPoint": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^EP\\d+$",
          "description": "MINTED BY `assemble` from content (source + trigger + owner + kind), never authored \u2014 leave it out of a fragment. Change-impact keeps its own content key (`ep:{source}`), which is stable across rebuilds where a minted number is not."
        },
        "kind": {
          "type": "string",
          "description": "what the entry point IS \u2014 a SEEDED-OPEN vocabulary (never blocking): prefer a seed (http-route, ui-route, cli, webhook, mcp-tool, middleware, job, poller, event-consumer, startup-hook, signal-handler); mint a project-specific kind only when none fits, and reuse the exact spelling on rebuild (validate folds known drift like 'http'\u2192'http-route' and nudges the rest)."
        },
        "trigger": {
          "type": "string"
        },
        "source": {
          "type": "string",
          "pattern": "^\\S+\\.\\w+(?::\\d+(?:-\\d+)?)?$|^\\S+:\\d+(?:-\\d+)?$",
          "description": "bare `path:line` anchor: a repo-relative file path, optionally followed by `:line` or `:line-line` \u2014 never a markdown link (its label would just be the basename, fully derivable from the path, so it is never authored)."
        },
        "component": {
          "type": "string",
          "pattern": "^C\\d+$",
          "description": "the owning component's id."
        },
        "activation": {
          "type": "string",
          "enum": [
            "self",
            "external",
            ""
          ],
          "description": "who starts it: 'self' (timer/loop/boot/signal/queue consumer \u2014 runs with no caller) or 'external' (route/CLI/callback/webhook); '' \u2192 inferred from `kind`."
        },
        "runs_in": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "cadence": {
          "type": "string",
          "description": "WHEN a self-activated entry point runs: a cron expression ('0 3 * * *'), an interval ('every 30s'), 'on-boot', or 'continuous'. Meaningful only when the effective activation is 'self' (a cadence on an external EP draws an advisory)."
        },
        "cadence_source": {
          "type": "string",
          "pattern": "^\\S+\\.\\w+(?::\\d+(?:-\\d+)?)?$|^\\S+:\\d+(?:-\\d+)?$",
          "description": "bare path:line anchor to the line DECLARING the schedule (beat/cron config, compose, the loop's sleep) \u2014 often a different line than `source`; '' on a set cadence = inferred (advisory)."
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "Entity": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^E\\d+$"
        },
        "name": {
          "type": "string"
        },
        "store": {
          "$ref": "#/$defs/Store"
        },
        "meaning": {
          "type": "string"
        },
        "subdomain": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^SD\\d+$",
          "description": "the owning subdomain's id, or null if ungrouped."
        },
        "source": {
          "type": [
            "string",
            "null"
          ],
          "description": "either a bare file `path:line` anchor (see `evidence[].file`'s description) or a bare directory ref ending in `/`. Must anchor the entity's actual type DEFINITION (the `class X`/`@dataclass` line), never a use site."
        },
        "fields": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/EntityField"
          }
        },
        "relations": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/EntityRelation"
          }
        },
        "states": {
          "$ref": "#/$defs/StateMachine"
        },
        "owners": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "pattern": "^CAP\\d+$"
          },
          "description": "OVERRIDE of the sub-domain's `owners`, for the one record whose owning feature differs from its area's \u2014 an audit entry sits in the Audit trail area but is written by the gateway. Author it ONLY where it differs from what would be inherited; an override equal to the inherited answer is reported as redundant. Absent = inherit, walking up `subdomain` then `parent`. Same vocabulary and same rules as `subdomains[].owners`."
        }
      },
      "required": [
        "id",
        "name"
      ],
      "additionalProperties": false
    },
    "Store": {
      "type": "object",
      "properties": {
        "dep": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^D\\d+$",
          "description": "the physical datastore/messaging dep holding this entity (a D-id); null for a store with no dep (in-memory, in-code registry)."
        },
        "container": {
          "type": "string",
          "description": "the compartment inside the dep: collection / table / key prefix / bucket / file name."
        },
        "mode": {
          "type": "string",
          "enum": [
            "collection",
            "embedded",
            "transient",
            "cache",
            "in-code",
            "enum",
            "projection",
            ""
          ],
          "description": "how the entity relates to its store \u2014 closed vocabulary, exact match: collection (own compartment) / embedded (PERSISTED inside a SAVED parent's row \u2014 a shape merely nested inside another in-memory object is NOT embedded, it takes its holder's own mode) / projection (a read view over rows another entity owns) / transient / cache / in-code / enum; '' = unstated."
        },
        "notes": {
          "type": "string",
          "description": "what the shape can't say: TTL, cache tiers, compression."
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "EntityField": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "type": {
          "type": "string"
        },
        "markers": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "annotation tokens, not free text: PK / FK\u2192En / unique / ? / []."
        }
      },
      "required": [
        "name"
      ],
      "additionalProperties": false
    },
    "EntityRelation": {
      "type": "object",
      "properties": {
        "verb": {
          "type": "string",
          "description": "contains / has / isA (structural, canonical) or a free association verb."
        },
        "target": {
          "type": "string",
          "pattern": "^E\\d+$"
        },
        "src_card": {
          "type": [
            "string",
            "null"
          ]
        },
        "dst_card": {
          "type": [
            "string",
            "null"
          ]
        },
        "display": {
          "type": "string"
        },
        "how": {
          "type": [
            "string",
            "null"
          ]
        },
        "keyed_by": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "storage key name(s) the store uses to relate the two \u2014 a lookup/partition key it imposes, NOT a field on EITHER entity's row. Use ONLY when no field backs the link; if a field carries the id (marked FK or a plain same-named column) that is a (reverse) foreign key, not a key. Drawn on the arrow with the \u00abkey\u00bb marker."
        }
      },
      "required": [
        "verb",
        "target"
      ],
      "additionalProperties": false
    },
    "NonEntityType": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "source": {
          "type": [
            "string",
            "null"
          ],
          "description": "either a bare file `path:line` anchor (see `evidence[].file`'s description) or a bare directory ref ending in `/`. Where the deliberately-unmodelled type is defined."
        },
        "why": {
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "additionalProperties": false
    },
    "Flow": {
      "type": "object",
      "properties": {
        "uc": {
          "type": "string",
          "pattern": "^UC\\d+$"
        },
        "title": {
          "type": "string"
        },
        "steps": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/FlowStep"
          }
        }
      },
      "required": [
        "uc",
        "title"
      ],
      "additionalProperties": false
    },
    "FlowStep": {
      "type": "object",
      "properties": {
        "n": {
          "type": "integer"
        },
        "src": {
          "type": "string",
          "description": "an element id, or a Role display name (an actor step)."
        },
        "dst": {
          "type": "string",
          "description": "same shape as `src`."
        },
        "phrase": {
          "type": "string"
        },
        "note": {
          "type": "string"
        },
        "where": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^\\S+\\.\\w+(?::\\d+(?:-\\d+)?)?$|^\\S+:\\d+(?:-\\d+)?$",
          "description": "bare `path:line` anchor: a repo-relative file path, optionally followed by `:line` or `:line-line` \u2014 never a markdown link (its label would just be the basename, fully derivable from the path, so it is never authored). THE location: this step's own call site \u2014 a step is exactly one interaction, so its anchor is precise (unlike an edge's `where`, an example). Required on element\u2194element steps unless `no_call_site`."
        },
        "no_call_site": {
          "type": "boolean",
          "description": "explicit opt-out (mirrors Edge.no_call_site): this step has no single call site \u2014 `where` may be null."
        },
        "direction": {
          "type": "string",
          "enum": [
            "in",
            "out",
            "both",
            ""
          ],
          "description": "WHICH WAY THE DATA MOVES, read from the PRODUCT's own code: in = it arrives, out = it leaves, both = one exchange runs each way. Owed where the map's OWN CODE touches a SURFACE (in = the product receives, out = it sends) or a RECORD (in = a read, out = a write); EMPTY on every other step, INCLUDING A DOOR \u2014 a role standing at a surface is a human action with no product end. It is the map's only statement of direction: the arrows between a component and a record say both read and write on half the record steps, and no arrow reaches a surface at all."
        },
        "subflow": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^SF\\d+$",
          "description": "a REFERENCE step: 'runs SFn here'. src/dst stay authored (the run's entry/exit endpoints); `phrase` may be empty (defaults to the sub-flow's name); the step carries no `where`/`no_call_site` of its own. One level only \u2014 a sub-flow's step may not itself reference a sub-flow."
        }
      },
      "required": [
        "n",
        "src",
        "dst"
      ],
      "additionalProperties": false
    },
    "SubFlow": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^SF\\d+$"
        },
        "name": {
          "type": "string",
          "description": "what the shared sequence does \u2014 a single verb phrase, like a use-case name."
        },
        "steps": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/FlowStep"
          }
        }
      },
      "required": [
        "id",
        "name"
      ],
      "additionalProperties": false
    },
    "Edge": {
      "type": "object",
      "properties": {
        "src": {
          "type": "string"
        },
        "verb": {
          "type": "string"
        },
        "dst": {
          "type": "string"
        },
        "why": {
          "type": [
            "string",
            "null"
          ],
          "description": "the relationship's rationale \u2014 distinct from either endpoint's own `purpose`."
        },
        "where": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^\\S+\\.\\w+(?::\\d+(?:-\\d+)?)?$|^\\S+:\\d+(?:-\\d+)?$",
          "description": "bare `path:line` anchor: a repo-relative file path, optionally followed by `:line` or `:line-line` \u2014 never a markdown link (its label would just be the basename, fully derivable from the path, so it is never authored). A verified EXAMPLE call site \u2014 one line in `src`'s code where it invokes `dst`, possibly one of many (a witness grounding the edge, not a catalog of its traffic)."
        },
        "no_call_site": {
          "type": "boolean"
        }
      },
      "required": [
        "src",
        "verb",
        "dst"
      ],
      "additionalProperties": false
    },
    "MessagingRow": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "unique channel/queue/topic name \u2014 the row's key (name-keyed like a deployment unit; nothing points at a channel)."
        },
        "kind": {
          "type": "string",
          "description": "seeded-open: queue / topic / stream / pubsub / job-queue; mint a project-specific kind when none fits."
        },
        "broker": {
          "type": "string",
          "pattern": "^D\\d+$",
          "description": "the messaging/datastore dep carrying the channel; '' = in-process."
        },
        "publishers": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "consumers": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "payload": {
          "type": "string",
          "pattern": "^E\\d+$",
          "description": "the entity a message carries; '' = untyped/none."
        },
        "source": {
          "type": "string",
          "pattern": "^\\S+\\.\\w+(?::\\d+(?:-\\d+)?)?$|^\\S+:\\d+(?:-\\d+)?$",
          "description": "bare path:line anchor to where the channel NAME is declared."
        }
      },
      "required": [
        "name"
      ],
      "additionalProperties": false
    },
    "DeploymentRow": {
      "type": "object",
      "properties": {
        "unit": {
          "type": "string"
        },
        "runs_on": {
          "type": "string"
        },
        "exposed_as": {
          "type": "string"
        },
        "config_source": {
          "type": "string"
        },
        "variants": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/VariantTag"
          }
        }
      },
      "required": [
        "unit"
      ],
      "additionalProperties": false
    },
    "VariantTag": {
      "type": "object",
      "properties": {
        "env": {
          "type": "string",
          "description": "the environment this deployment unit runs in \u2014 must name a `environments` entry."
        },
        "source": {
          "type": "string",
          "pattern": "^\\S+\\.\\w+(?::\\d+(?:-\\d+)?)?$|^\\S+:\\d+(?:-\\d+)?$",
          "description": "bare `path:line` anchor: a repo-relative file path, optionally followed by `:line` or `:line-line` \u2014 never a markdown link (its label would just be the basename, fully derivable from the path, so it is never authored). The manifest line that PLACES the unit in this environment (the compose `profiles:` line, the overlay/values file, the stage). Empty string = INFERRED (no manifest witness): `validate` surfaces it as an advisory, never blocks; a CITED source that doesn't resolve IS a hard block under `--check-sources`."
        }
      },
      "required": [
        "env"
      ],
      "additionalProperties": false
    },
    "ObservabilityRow": {
      "type": "object",
      "properties": {
        "signal": {
          "type": "string"
        },
        "where_emitted": {
          "type": "string"
        },
        "where_viewed": {
          "type": "string"
        },
        "alerts": {
          "type": "string"
        }
      },
      "required": [
        "signal"
      ],
      "additionalProperties": false
    },
    "SecurityRow": {
      "type": "object",
      "properties": {
        "surface": {
          "type": "string"
        },
        "who": {
          "type": "string"
        },
        "source": {
          "type": "string",
          "pattern": "^\\S+\\.\\w+(?::\\d+(?:-\\d+)?)?$|^\\S+:\\d+(?:-\\d+)?$",
          "description": "bare `path:line` anchor: a repo-relative file path, optionally followed by `:line` or `:line-line` \u2014 never a markdown link (its label would just be the basename, fully derivable from the path, so it is never authored). The auth check in code (the enforcement site)."
        },
        "risk": {
          "type": "string"
        }
      },
      "required": [
        "surface"
      ],
      "additionalProperties": false
    },
    "ConfigRow": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string"
        },
        "purpose": {
          "type": "string"
        },
        "default": {
          "type": "string"
        },
        "per_env": {
          "type": "string"
        }
      },
      "required": [
        "key"
      ],
      "additionalProperties": false
    },
    "TestRow": {
      "type": "object",
      "properties": {
        "targets": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "tested": {
          "type": "string"
        },
        "label": {
          "type": "string"
        },
        "tests": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/EvidenceItem"
          }
        },
        "gap": {
          "type": "string"
        },
        "confidence": {
          "type": "string",
          "enum": [
            "verified",
            "inferred",
            ""
          ],
          "description": "verified = read in the code; inferred = deduced. '' = unstated."
        }
      },
      "required": [
        "targets"
      ],
      "additionalProperties": false
    },
    "Grounding": {
      "type": "object",
      "properties": {
        "claims_total": {
          "type": "integer"
        },
        "claims_challenged": {
          "type": "integer"
        },
        "claims_confirmed": {
          "type": "integer"
        },
        "claims_refuted": {
          "type": "integer"
        },
        "claims_unverifiable": {
          "type": "integer"
        },
        "claims_superseded": {
          "type": "integer"
        },
        "claims_added_since": {
          "type": "integer"
        },
        "live_claims_digest": {
          "type": "string"
        },
        "claims_live_challenged": {
          "type": "integer"
        },
        "closer_upheld": {
          "type": "integer"
        },
        "closer_rejected": {
          "type": "integer"
        },
        "closer_unsure": {
          "type": "integer"
        },
        "closer_disputed": {
          "type": "integer"
        },
        "note": {
          "type": "string"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "BusinessRule": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^BR\\d+$"
        },
        "statement": {
          "type": "string",
          "description": "ONE product decision, in product language and naming no component \u2014 the sharp test is 'could a product person have decided otherwise?'. Two claims joined by 'and' are two rules."
        },
        "name": {
          "type": "string",
          "description": "the SHORT title \u2014 a few words a reader scans, the way a use case has a name beside its trigger\u2192outcome sentence (\"Owner-only cancellation\"). Not a shortened statement: name the DECISION, then state it in full in `statement`."
        },
        "block": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^BLK\\d+$",
          "description": "the decision area this rule belongs to, or null. Assigned at synthesis via `reconcile` (a `BLK<n>` does not exist when the rule is authored), exactly as `use_cases[].capability` is."
        },
        "sites": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/RuleSite"
          },
          "description": "every place the decision is ENFORCED. The rule's components, its use-case steps and whether it has been swept are DERIVED from these \u2014 there is no authored field for any of them."
        },
        "access": {
          "type": "boolean",
          "description": "this rule governs WHO MAY DO WHAT \u2014 the security marker. The security surface table and the eval's auth coverage read it."
        },
        "risk": {
          "type": "string",
          "description": "what is AT STAKE if this decision is wrong or absent \u2014 a judgement, not a derivation. Distinct from a site's `why` (what that LINE does): a risk note says what the decision's limit costs. Rendered in the security surface table for an `access` rule."
        },
        "confidence": {
          "type": "string",
          "enum": [
            "verified",
            "inferred",
            ""
          ],
          "description": "verified = read in the code; inferred = deduced. '' = unstated."
        }
      },
      "required": [
        "id",
        "statement",
        "name"
      ],
      "additionalProperties": false
    },
    "RuleSite": {
      "type": "object",
      "properties": {
        "where": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^\\S+:\\d+(?:-\\d+)?$",
          "description": "bare `path:line` anchor: a repo-relative file path, optionally followed by `:line` or `:line-line` \u2014 never a markdown link (its label would just be the basename, fully derivable from the path, so it is never authored). The OPERATIVE line \u2014 the one that acts. A definition header, an import, a comment or a blank line is a shape error, not a site, and the `:line` is REQUIRED: without it the operative-line check is skipped and the claim cannot be falsified. null (with `no_call_site`) = enforced by construction, no single line."
        },
        "why": {
          "type": "string",
          "description": "what this line does FOR the rule ('rejects a non-owner caller') \u2014 reconstructible from the line itself, with no clause added."
        },
        "no_call_site": {
          "type": "boolean",
          "description": "explicit opt-out (mirrors Edge.no_call_site): this rule is enforced by construction (a type, a schema constraint, a config-wired guard) \u2014 `where` may be empty."
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "ExtraSection": {
      "type": "object",
      "properties": {
        "heading": {
          "type": "string"
        },
        "body": {
          "type": "string"
        }
      },
      "required": [
        "heading"
      ],
      "additionalProperties": false
    }
  }
}
